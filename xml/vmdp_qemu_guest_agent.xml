<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE sect1 PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd"
[
  <!ENTITY % NOVDOC.DEACTIVATE.IDREF "INCLUDE">
  <!ENTITY % entities SYSTEM "entity-decl.ent">
  %entities;
]>


<sect1 id="sec.vmdp.qemu.guest.agent">
 <title>QEMU Guest Agent</title>
 <para>This section explain what's <emphasis>qemu-ga</emphasis> and how to install it.</para>
 <sect2 id="vmdp.qemu.guest.agent.info">
  <title>Qemu guest agent</title>
  <para>
   VMDP setup allows for the installation of the qemu guest agent service for
   Windows* when invoked with the <command>/qemu-ga</command> command line parameter. The qemu
   guest agent requires the VM to be equipped with the qemu guest agent
   channel (see <xref linkend="vmdp.qemu.guest.agent.channel" xrefstyle="SectTitleOnPage"/>). Without the <emphasis>qemu guest agent channel</emphasis>,
   the qemu guest agent can install but will not load. Trying to load without
   the channel results in errors in the event log.
  </para>
  <para>
   The qemu guest agent is built in the <emphasis>mingw</emphasis> environment
   on a Linux system with no modifications to the guest agent source code.
   The guest agent and supporting DLLs remain under their respective GPL licenses.
  </para>
  <para>
   Currently many of the features of the guest agent supported in Linux vms have
   not yet been implemented for Windows*. So usage of guest agent in
   Windows* is somewhat limited at present.
  </para>
 </sect2>
 
 <sect2 id="sec.vmdp.qemu.guest.agent.installation">
  <title>Qemu guest agent installation</title>
  <para>
   Open a <guimenu>cmd.exe</guimenu> from the start menu.
   Go to the path which contains the <filename>qemu-ga</filename> directory.
   The <command>qemu-ga.exe</command> <option>--help</option> command will show you the help.
   To install the service use:
  </para>
  <screen>qemu-ga.exe --service install</screen>
  <para>To start the service you can use the Windows* Service Administrative tools, or the command line:</para>
  <screen>net start qemu-ga.exe</screen>
  <informalfigure pgwide="0">
   <mediaobject>
    <imageobject role="fo">
     <imagedata fileref="vmdp_qemu-ga.png" width="56%" format="PNG"/>
    </imageobject>
    <imageobject role="html">
     <imagedata fileref="vmdp_qemu-ga.png" width="56%" format="PNG"/>
    </imageobject>
   </mediaobject>
  </informalfigure>
 </sect2>
</sect1>
